import React from 'react';
import { Link } from 'react-router';
import Hero from '../Components/Hero';

const Home = () => {
    return (
        <div>
            <Hero/>
        </div>
    );
};

export default Home;